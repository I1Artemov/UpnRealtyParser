1) Открыть страницу с таблицей квартир (https://upn.ru/realty_eburg_flat_sale.htm)
2) Забрать всю информацию из таблицы.
	2.1) Статус GatheringLinks.
	2.2) Посчитать, сколько всего записей о квартирах в таблице: UpnFlatLinksCollector.GetTotalEntriesInTable(webPageText).
	2.3) Посчитать, сколько страниц в таблице UpnFlatLinksCollector.GetMaxPagesInTable(totalApartmentsAmount.GetValueOrDefault(0)) (кол-во записей делится на 30).
	2.4) В цикле пройтись по каждой странице таблице.
		2.4.1) Перейти на очередную страницу - НЕРЕАЛИЗОВАНО, нужен УРЛ. Как реализовать: взять URL с кнопки с номером первой страницы, забрать из него sid. Для перехода на нужную страницу вставлять в &id={1} значение.
		2.4.2) Забрать все ссылки на квартиры со страницы: UpnFlatLinksCollector.GetLinksFromSinglePage(webPageText);
		2.4.3) Массив ссылок заинсертить в БД (PageLink: LinkType = "SellFlat").
3) Пройтись по каждой квартире из списка ссылок
3.1) Статус ObservingFlat, в Details - ссылка на квартиру.
3.2) Проверяем, есть ли уже эта квартира в БД. Смотрим, есть ли в [UpnFlat] такой же [SiteUrl].
3.3) Если квартира уже есть, то проверяем изменения (НЕ РЕАЛИЗОВАНО)
3.3.1) Пишем историю изменений в отдельную таблицу (НЕ РЕАЛИЗОВАНО)
3.3.2) Обновляем значение, сохраняем в БД.
3.4) Если квартира новая, то:
3.4.1) Берем информацию о доме UpnHouseParser.GetUpnHouseFromPageText
3.4.2) Проверяем по адресу, есть ли уже такой дом (НЕ РЕАЛИЗОВАНО). Если нет, то добавляем в БД. Если есть, то ставим существующий Id квартире.
3.4.3) Берем информацию по самой квартире. Квартира 99% новая, т.к. новый URL, но возможно, что выставлена на продажу второй раз. Добавляем квартртиру в БД.